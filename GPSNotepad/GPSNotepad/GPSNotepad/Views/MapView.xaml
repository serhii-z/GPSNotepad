<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:GPSNotepad.Controls"
             x:Class="GPSNotepad.Views.MapView"
             Title="Map">

    <AbsoluteLayout>
        <SearchBar Placeholder="Search pin" 
                   AbsoluteLayout.LayoutFlags="All" 
                   AbsoluteLayout.LayoutBounds="0,0,1,0.08"></SearchBar>
        <ListView  AbsoluteLayout.LayoutFlags="All" 
                   AbsoluteLayout.LayoutBounds="0,0.36,1,0.74"
                  ItemsSource="{Binding SearchResults}" />
        <local:CustomMap
            AbsoluteLayout.LayoutFlags="All"
            AbsoluteLayout.LayoutBounds="0,1,1,0.91"
            PinsSource="{Binding Pins}"
            CommandPinTap="{Binding PinTappedCommand}"
            Region="{Binding Region}"
            Animated="{Binding Animated}">
        </local:CustomMap>
    </AbsoluteLayout>
</ContentPage>